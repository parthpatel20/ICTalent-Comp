apiVersion: v1
kind: Service
metadata:
  name: {{ template "talentservicesidentity.fullname" . }}
  labels:
    app: {{ template "talentservicesidentity.name" . }}
    chart: {{ template "talentservicesidentity.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "talentservicesidentity.name" . }}
    release: {{ .Release.Name }}
